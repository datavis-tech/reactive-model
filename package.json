{
  "name": "reactive-model",
  "version": "0.6.0",
  "description": "A library for data flow components.",
  "main": "reactive-model.js",
  "scripts": {
    "test": "mocha",
    "build": "VERSION=`node -e 'console.log(require(\"./package.json\").version)'`; browserify index.js -s ReactiveModel | tee reactive-model-v$VERSION.js | uglifyjs -cm > reactive-model-v$VERSION-min.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/curran/reactive-model.git"
  },
  "author": "Curran Kelleher",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/curran/reactive-model/issues"
  },
  "homepage": "https://github.com/curran/reactive-model",
  "devDependencies": {
    "mocha": "^2.4.5"
  },
  "dependencies": {
    "reactive-function": "^0.9.0",
    "reactive-property": "^0.8.0"
  }
}
